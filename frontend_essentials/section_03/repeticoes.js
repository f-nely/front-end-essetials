// for of
let number = [2, 4, 3, 5, 6];

// for of (valores do array)
for (num of number) {
    console.log(num);
}

console.log('');

// for in
let animals = {
    name: 'lion',
    wight: 200,
    age: 4
}

for (item in animals) {
    console.log(animals[item]);
}